from setuptools import setup, find_packages

setup(
    name='nomedelpacchetto',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Lista delle dipendenze del tuo pacchetto
    ],
    entry_points={
        'console_scripts': [
            # Eventuali script eseguibili
        ],
    },
    # Altre informazioni come autore, email, descrizione, ecc.
)
